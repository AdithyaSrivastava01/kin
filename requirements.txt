# HealthSwarm Python dependencies — local development.
#
# Install:
#   python -m pip install -r requirements.txt
#
# Excludes Vultr-only ML deps (torch, transformers, timm, accelerate) —
# those install on the GPU box only via vultr/gemma_server.py setup.

# --- Core / shared ---
pymongo>=4.7
dnspython>=2.6                 # SRV resolution for mongodb+srv://
python-dotenv>=1.0
requests>=2.31
certifi>=2024.2.2              # fresh CA bundle for Atlas/Overpass TLS

# --- Dashboard relay (FastAPI + GridFS uploads) ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # multipart form parsing for /upload

# --- LLM clients ---
openai>=1.40                   # ASI:One uses OpenAI-compatible API

# --- E1 agents (Fetch.ai uAgents) ---
uagents>=0.19

# --- E2 voice gateway ---
twilio>=9.0
elevenlabs>=1.5
pyngrok>=7.2                   # optional: NGROK_AUTOSTART=true opens the tunnel from Python
