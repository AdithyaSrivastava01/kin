# HealthSwarm Python dependencies — local development.
#
# Install (Python 3.10+; tested on 3.12):
#   py -3.12 -m pip install -r requirements.txt
#
# Versions are pinned to the set verified working with the voice gateway,
# uAgents runners, and dashboard relay. Pinning means subsequent installs
# hit pip's wheel cache and skip dependency resolution.
#
# Speech-to-text is handled by ElevenLabs Scribe (already pulled in via
# the elevenlabs SDK) — no separate STT or GPU service required.

# --- Core / shared ---
pymongo==4.17.0
dnspython==2.8.0                # SRV resolution for mongodb+srv://
python-dotenv==1.1.1
requests==2.33.1
certifi==2026.4.22              # fresh CA bundle for Atlas/Overpass TLS

# --- Dashboard relay (FastAPI + GridFS uploads) ---
fastapi==0.119.0
uvicorn[standard]==0.37.0
python-multipart==0.0.26        # multipart form parsing for /upload

# --- LLM clients ---
openai==2.32.0                  # ASI:One uses OpenAI-compatible API

# --- E1 agents (Fetch.ai uAgents) ---
uagents==0.24.2

# --- Voice gateway ---
# Outbound calls go through ElevenLabs Conversational AI (which handles
# Twilio + STT + LLM + TTS server-side). We only need the SDK.
elevenlabs==2.44.0
